{\rtf1\ansi\ansicpg1252\cocoartf2822
\cocoatextscaling0\cocoaplatform0{\fonttbl\f0\fswiss\fcharset0 Helvetica;}
{\colortbl;\red255\green255\blue255;}
{\*\expandedcolortbl;;}
\paperw11900\paperh16840\margl1440\margr1440\vieww11520\viewh8400\viewkind0
\pard\tx720\tx1440\tx2160\tx2880\tx3600\tx4320\tx5040\tx5760\tx6480\tx7200\tx7920\tx8640\pardirnatural\partightenfactor0

\f0\fs24 \cf0 <!DOCTYPE html>\
<html lang="ca">\
<head>\
    <meta charset="UTF-8">\
    <meta name="viewport" content="width=device-width, initial-scale=1.0">\
    <title>TravelMate - El teu company de viatge intel\'b7ligent</title>\
    <script src="https://cdn.tailwindcss.com"></script>\
    <link href="https://fonts.googleapis.com/css2?family=Plus+Jakarta+Sans:wght@400;500;600;700;800&display=swap" rel="stylesheet">\
    <script src="https://unpkg.com/lucide@latest"></script>\
    <style>\
        body \{\
            font-family: 'Plus Jakarta Sans', sans-serif;\
            background-color: #f8fafc; /* slate-50 */\
            color: #0f172a;\
        \}\
        .glass \{\
            background: rgba(255, 255, 255, 0.85);\
            backdrop-filter: blur(12px);\
            -webkit-backdrop-filter: blur(12px);\
            border: 1px solid rgba(255, 255, 255, 0.5);\
        \}\
        .no-scrollbar::-webkit-scrollbar \{ display: none; \}\
        .no-scrollbar \{ -ms-overflow-style: none; scrollbar-width: none; \}\
        @keyframes float \{\
            0%, 100% \{ transform: translateY(0); \}\
            50% \{ transform: translateY(-10px); \}\
        \}\
        .animate-float \{ animation: float 4s ease-in-out infinite; \}\
        .gradient-text \{\
            background: linear-gradient(135deg, #4f46e5 0%, #7c3aed 100%);\
            -webkit-background-clip: text;\
            -webkit-text-fill-color: transparent;\
        \}\
    </style>\
</head>\
<body class="min-h-screen">\
\
    <!-- App Root -->\
    <div id="app" class="relative pb-32">\
        <!-- Pantalla de C\'e0rrega (Overlay) -->\
        <div id="loading-overlay" class="hidden fixed inset-0 z-[100] bg-white/90 backdrop-blur-md flex flex-col items-center justify-center">\
            <div class="w-20 h-20 border-4 border-indigo-200 border-t-indigo-600 rounded-full animate-spin mb-4"></div>\
            <p class="font-bold text-indigo-900 animate-pulse">Preparant la teva aventura...</p>\
        </div>\
\
        <!-- Secci\'f3: Landing -->\
        <section id="view-landing" class="h-screen flex flex-col items-center justify-center p-6 text-center">\
            <div class="w-20 h-20 bg-indigo-600 rounded-3xl flex items-center justify-center text-white text-4xl shadow-2xl mb-8 animate-float">\
                <i data-lucide="map"></i>\
            </div>\
            <h1 class="text-6xl font-extrabold tracking-tighter mb-4 gradient-text">TravelMate</h1>\
            <p class="text-slate-500 text-lg font-medium max-w-sm mb-10">El teu company de viatge intel\'b7ligent per a experi\'e8ncies \'faniques.</p>\
            <button onclick="showView('dashboard')" class="bg-indigo-600 text-white px-8 py-4 rounded-2xl font-bold text-lg shadow-xl shadow-indigo-200 hover:scale-105 active:scale-95 transition-all">\
                Comen\'e7ar aventura\
            </button>\
        </section>\
\
        <!-- Secci\'f3: Dashboard -->\
        <section id="view-dashboard" class="hidden max-w-4xl mx-auto p-6 pt-20">\
            <div class="flex justify-between items-end mb-12">\
                <div>\
                    <h2 class="text-4xl font-extrabold tracking-tight">Els meus viatges</h2>\
                    <p class="text-indigo-500 font-bold text-xs uppercase tracking-widest mt-1">Explora els teus itineraris</p>\
                </div>\
                <button onclick="showView('form')" class="w-14 h-14 bg-indigo-600 text-white rounded-2xl flex items-center justify-center shadow-lg hover:rotate-90 transition-all">\
                    <i data-lucide="plus"></i>\
                </button>\
            </div>\
            <div id="trips-grid" class="grid grid-cols-1 md:grid-cols-2 gap-6">\
                <!-- Aqu\'ed es carreguen els viatges -->\
            </div>\
            <div id="empty-state" class="hidden text-center py-20 bg-white/50 rounded-[40px] border-2 border-dashed border-slate-200">\
                <p class="text-slate-400 font-bold">Encara no tens cap viatge creat.</p>\
            </div>\
        </section>\
\
        <!-- Secci\'f3: Formulari -->\
        <section id="view-form" class="hidden max-w-xl mx-auto p-6 pt-20">\
            <button onclick="showView('dashboard')" class="flex items-center gap-2 text-indigo-600 font-bold text-sm mb-8">\
                <i data-lucide="chevron-left" class="w-4 h-4"></i> Tornar\
            </button>\
            <div class="glass p-8 rounded-[40px] shadow-2xl space-y-8">\
                <h3 class="text-2xl font-extrabold">Nou Itinerari</h3>\
                <div class="space-y-4">\
                    <label class="text-xs font-black uppercase text-slate-400 tracking-widest ml-1">Destinaci\'f3</label>\
                    <input id="form-dest" type="text" placeholder="Ex: T\'f2quio, Roma..." class="w-full bg-slate-100 p-5 rounded-2xl border-none focus:ring-2 focus:ring-indigo-500 font-bold">\
                </div>\
                <div class="grid grid-cols-2 gap-4">\
                    <div class="space-y-4">\
                        <label class="text-xs font-black uppercase text-slate-400 tracking-widest ml-1">Dies</label>\
                        <input id="form-days" type="number" value="3" min="1" max="10" class="w-full bg-slate-100 p-5 rounded-2xl border-none font-bold">\
                    </div>\
                    <div class="space-y-4">\
                        <label class="text-xs font-black uppercase text-slate-400 tracking-widest ml-1">Tipus</label>\
                        <select id="form-type" class="w-full bg-slate-100 p-5 rounded-2xl border-none font-bold">\
                            <option value="Cultural">Cultural</option>\
                            <option value="Aventura">Aventura</option>\
                            <option value="Relax">Relax</option>\
                        </select>\
                    </div>\
                </div>\
                <button onclick="generateTrip()" class="w-full bg-indigo-600 text-white py-6 rounded-2xl font-bold text-xl shadow-xl hover:bg-indigo-700 transition-all">\
                    Generar Viatge \uc0\u10024 \
                </button>\
            </div>\
        </section>\
\
        <!-- Secci\'f3: Detall del Viatge -->\
        <section id="view-detail" class="hidden">\
            <div id="trip-header" class="relative h-[50vh] overflow-hidden">\
                <img id="trip-img" src="" class="w-full h-full object-cover">\
                <div class="absolute inset-0 bg-gradient-to-t from-slate-50 via-transparent to-black/20"></div>\
                <button onclick="showView('dashboard')" class="absolute top-8 left-8 w-12 h-12 bg-white/90 rounded-xl flex items-center justify-center shadow-xl">\
                    <i data-lucide="home"></i>\
                </button>\
                <div class="absolute bottom-10 left-10 right-10">\
                    <h2 id="trip-title-name" class="text-5xl font-extrabold text-slate-900 tracking-tighter"></h2>\
                    <p id="trip-subtitle" class="text-indigo-600 font-bold text-lg mt-1"></p>\
                </div>\
            </div>\
\
            <div class="max-w-4xl mx-auto px-6 -mt-8 relative z-10">\
                <div class="glass p-4 rounded-3xl flex gap-2 mb-10 overflow-x-auto no-scrollbar">\
                    <button onclick="setTab('itinerary')" class="tab-btn active bg-indigo-600 text-white px-8 py-3 rounded-2xl font-bold text-xs uppercase tracking-widest">Itinerari</button>\
                    <button onclick="setTab('packing')" class="tab-btn text-slate-400 px-8 py-3 rounded-2xl font-bold text-xs uppercase tracking-widest">Maleta</button>\
                    <button onclick="setTab('spots')" class="tab-btn text-slate-400 px-8 py-3 rounded-2xl font-bold text-xs uppercase tracking-widest">Llocs</button>\
                </div>\
\
                <div id="tab-itinerary" class="tab-content space-y-8 pb-20">\
                    <!-- Itinerari din\'e0mic -->\
                </div>\
                <div id="tab-packing" class="tab-content hidden space-y-4 pb-20">\
                    <!-- Llista de maleta -->\
                </div>\
                <div id="tab-spots" class="tab-content hidden space-y-4 pb-20">\
                    <!-- Llocs imprescindibles -->\
                </div>\
            </div>\
        </section>\
\
        <!-- Bot\'f3 Xat i Modal -->\
        <button id="chat-fab" onclick="toggleChat()" class="hidden fixed bottom-8 right-8 w-16 h-16 bg-indigo-600 text-white rounded-2xl shadow-2xl flex items-center justify-center z-[80] hover:scale-110 transition-all">\
            <i data-lucide="sparkles"></i>\
        </button>\
\
        <div id="chat-modal" class="hidden fixed inset-0 z-[100] flex items-end justify-center p-4 bg-slate-900/40 backdrop-blur-sm">\
            <div class="w-full max-w-lg bg-white rounded-[40px] shadow-2xl overflow-hidden flex flex-col h-[70vh]">\
                <div class="bg-indigo-600 p-6 text-white flex justify-between items-center">\
                    <div class="flex items-center gap-3">\
                        <i data-lucide="bot"></i>\
                        <span class="font-bold">Guia Intel\'b7ligent</span>\
                    </div>\
                    <button onclick="toggleChat()"><i data-lucide="x"></i></button>\
                </div>\
                <div id="chat-messages" class="flex-1 overflow-y-auto p-6 space-y-4 bg-slate-50">\
                    <!-- Missatges -->\
                </div>\
                <div class="p-6 bg-white border-t flex gap-3">\
                    <input id="chat-input" type="text" placeholder="Pregunta el que vulguis..." class="flex-1 bg-slate-100 p-4 rounded-xl border-none font-medium focus:ring-2 focus:ring-indigo-500">\
                    <button onclick="sendMessage()" class="bg-indigo-600 text-white p-4 rounded-xl shadow-lg"><i data-lucide="send"></i></button>\
                </div>\
            </div>\
        </div>\
\
        <!-- Navigation Bar (Mobile Style) -->\
        <nav id="bottom-nav" class="hidden fixed bottom-8 left-8 right-8 h-20 glass rounded-[30px] flex items-center justify-around px-6 z-50">\
            <button onclick="showView('dashboard')" class="p-3 rounded-xl text-slate-400 hover:text-indigo-600 transition-all">\
                <i data-lucide="layout-grid"></i>\
            </button>\
            <button onclick="showView('form')" class="w-16 h-16 -mt-16 bg-indigo-600 text-white rounded-2xl flex items-center justify-center shadow-xl shadow-indigo-200 border-4 border-slate-50">\
                <i data-lucide="plus"></i>\
            </button>\
            <button onclick="if(currentTrip) showView('detail')" class="p-3 rounded-xl text-slate-400 hover:text-indigo-600 transition-all">\
                <i data-lucide="map-pin"></i>\
            </button>\
        </nav>\
    </div>\
\
    <script>\
        // --- Estat de l'Aplicaci\'f3 ---\
        let trips = JSON.parse(localStorage.getItem('tm_trips') || '[]');\
        let currentTrip = null;\
        const apiKey = ""; // La clau s'injecta autom\'e0ticament a l'entorn\
\
        // --- Inicialitzaci\'f3 ---\
        window.onload = () => \{\
            lucide.createIcons();\
            renderTrips();\
        \};\
\
        // --- Navegaci\'f3 ---\
        function showView(viewId) \{\
            document.querySelectorAll('section').forEach(s => s.classList.add('hidden'));\
            document.getElementById(`view-$\{viewId\}`).classList.remove('hidden');\
            \
            const nav = document.getElementById('bottom-nav');\
            const fab = document.getElementById('chat-fab');\
            \
            if (viewId === 'landing') \{\
                nav.classList.add('hidden');\
                fab.classList.add('hidden');\
            \} else \{\
                nav.classList.remove('hidden');\
                if (viewId === 'detail') fab.classList.remove('hidden');\
                else fab.classList.add('hidden');\
            \}\
            window.scrollTo(0,0);\
        \}\
\
        function setTab(tabId) \{\
            document.querySelectorAll('.tab-content').forEach(c => c.classList.add('hidden'));\
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.remove('active', 'bg-indigo-600', 'text-white'));\
            document.querySelectorAll('.tab-btn').forEach(b => b.classList.add('text-slate-400'));\
\
            document.getElementById(`tab-$\{tabId\}`).classList.remove('hidden');\
            const activeBtn = Array.from(document.querySelectorAll('.tab-btn')).find(b => b.innerText.toLowerCase().includes(tabId === 'itinerary' ? 'itinerari' : tabId));\
            if(activeBtn) \{\
                activeBtn.classList.add('active', 'bg-indigo-600', 'text-white');\
                activeBtn.classList.remove('text-slate-400');\
            \}\
        \}\
\
        // --- L\'f2gica de Viatges ---\
        async function generateTrip() \{\
            const dest = document.getElementById('form-dest').value;\
            const days = document.getElementById('form-days').value;\
            const type = document.getElementById('form-type').value;\
\
            if (!dest) return;\
\
            const loader = document.getElementById('loading-overlay');\
            loader.classList.remove('hidden');\
\
            const prompt = `Actua com un guia de viatges professional. Crea un itinerari de $\{days\} dies a $\{dest\} amb un estil $\{type\}. \
            Respon EXCLUSIVAMENT en format JSON v\'e0lid amb aquesta estructura:\
            \{\
                "title": "T\'edtol curt",\
                "description": "Descripci\'f3 curta",\
                "days": [\{"day": 1, "title": "Tema", "activities": ["act 1", "act 2"]\}],\
                "packing": ["item 1", "item 2"],\
                "essentials": ["lloc 1", "lloc 2"]\
            \}\
            Tota la informaci\'f3 ha d'estar en catal\'e0.`;\
\
            try \{\
                // 1. Contingut\
                const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=$\{apiKey\}`, \{\
                    method: 'POST',\
                    headers: \{'Content-Type': 'application/json'\},\
                    body: JSON.stringify(\{\
                        contents: [\{ parts: [\{ text: prompt \}] \}],\
                        generationConfig: \{ responseMimeType: "application/json" \}\
                    \})\
                \});\
                const data = await response.json();\
                const result = JSON.parse(data.candidates[0].content.parts[0].text);\
\
                // 2. Imatge (Fallback si falla Imagen)\
                let imageUrl = `https://images.unsplash.com/photo-1476514525535-07fb3b4ae5f1?q=80&w=1000&auto=format&fit=crop`;\
                try \{\
                    const imgResponse = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/imagen-4.0-generate-001:predict?key=$\{apiKey\}`, \{\
                        method: 'POST',\
                        body: JSON.stringify(\{\
                            instances: \{ prompt: `Cinematic travel photography of $\{dest\}, professional lighting, 4k` \},\
                            parameters: \{ sampleCount: 1 \}\
                        \})\
                    \});\
                    const imgData = await imgResponse.json();\
                    if(imgData.predictions?.[0]?.bytesBase64Encoded) \{\
                        imageUrl = `data:image/png;base64,$\{imgData.predictions[0].bytesBase64Encoded\}`;\
                    \}\
                \} catch(e) \{ console.error("Error imatge", e); \}\
\
                const newTrip = \{ ...result, id: Date.now(), dest, imageUrl, type \};\
                trips.unshift(newTrip);\
                localStorage.setItem('tm_trips', JSON.stringify(trips));\
                \
                renderTrips();\
                openTrip(newTrip.id);\
            \} catch (error) \{\
                console.error(error);\
                alert("S'ha produ\'eft un error al generar el viatge.");\
            \} finally \{\
                loader.classList.add('hidden');\
            \}\
        \}\
\
        function renderTrips() \{\
            const grid = document.getElementById('trips-grid');\
            const empty = document.getElementById('empty-state');\
            grid.innerHTML = '';\
\
            if (trips.length === 0) \{\
                empty.classList.remove('hidden');\
                return;\
            \}\
            empty.classList.add('hidden');\
\
            trips.forEach(trip => \{\
                const card = document.createElement('div');\
                card.className = "group relative bg-white rounded-[32px] overflow-hidden shadow-sm hover:shadow-xl transition-all border border-slate-100 cursor-pointer";\
                card.innerHTML = `\
                    <div onclick="openTrip($\{trip.id\})" class="h-56 relative overflow-hidden">\
                        <img src="$\{trip.imageUrl\}" class="w-full h-full object-cover group-hover:scale-110 transition-transform duration-700">\
                        <div class="absolute inset-0 bg-gradient-to-t from-black/60 to-transparent"></div>\
                        <div class="absolute bottom-5 left-6 right-6 text-white">\
                            <h4 class="font-bold text-xl">$\{trip.dest\}</h4>\
                            <p class="text-[10px] font-bold uppercase tracking-widest opacity-80">$\{trip.days.length\} dies \'b7 $\{trip.type\}</p>\
                        </div>\
                    </div>\
                    <button onclick="deleteTrip($\{trip.id\})" class="absolute top-4 right-4 w-8 h-8 bg-black/20 backdrop-blur-md rounded-lg text-white opacity-0 group-hover:opacity-100 hover:bg-red-500 transition-all">\uc0\u10005 </button>\
                `;\
                grid.appendChild(card);\
            \});\
        \}\
\
        function openTrip(id) \{\
            currentTrip = trips.find(t => t.id === id);\
            if (!currentTrip) return;\
\
            document.getElementById('trip-img').src = currentTrip.imageUrl;\
            document.getElementById('trip-title-name').innerText = currentTrip.dest;\
            document.getElementById('trip-subtitle').innerText = currentTrip.title;\
\
            // Render Itinerari\
            const itin = document.getElementById('tab-itinerary');\
            itin.innerHTML = `<div class="bg-white p-8 rounded-[32px] shadow-sm italic text-slate-500 text-center font-medium">"$\{currentTrip.description\}"</div>`;\
            currentTrip.days.forEach(d => \{\
                const dayDiv = document.createElement('div');\
                dayDiv.innerHTML = `\
                    <h5 class="text-2xl font-black text-slate-900 mb-4">Dia $\{d.day\}: $\{d.title\}</h5>\
                    <div class="space-y-4">\
                        $\{d.activities.map(a => `\
                            <div class="bg-white p-5 rounded-2xl shadow-sm border border-slate-50 flex items-start gap-4">\
                                <span class="text-indigo-600 mt-1"><i data-lucide="check-circle-2" class="w-5 h-5"></i></span>\
                                <span class="font-bold text-slate-700">$\{a\}</span>\
                            </div>\
                        `).join('')\}\
                    </div>\
                `;\
                itin.appendChild(dayDiv);\
            \});\
\
            // Render Packing\
            const pack = document.getElementById('tab-packing');\
            pack.innerHTML = '<h5 class="text-2xl font-black mb-6">Llista de Maleta</h5>';\
            const packGrid = document.createElement('div');\
            packGrid.className = "grid grid-cols-2 gap-3";\
            currentTrip.packing.forEach(item => \{\
                const i = document.createElement('div');\
                i.className = "bg-white p-4 rounded-xl shadow-sm font-bold text-sm text-slate-600 flex items-center gap-3";\
                i.innerHTML = `<input type="checkbox" class="w-5 h-5 rounded-lg border-slate-200 text-indigo-600"> $\{item\}`;\
                packGrid.appendChild(i);\
            \});\
            pack.appendChild(packGrid);\
\
            // Render Spots\
            const spots = document.getElementById('tab-spots');\
            spots.innerHTML = '<h5 class="text-2xl font-black mb-6">Imprescindibles</h5>';\
            currentTrip.essentials.forEach(e => \{\
                const s = document.createElement('div');\
                s.className = "bg-indigo-600 p-6 rounded-2xl text-white shadow-lg flex justify-between items-center";\
                s.innerHTML = `<div><p class="text-[10px] font-black uppercase opacity-60">Top Spot</p><p class="font-bold text-lg">$\{e\}</p></div><i data-lucide="camera"></i>`;\
                spots.appendChild(s);\
            \});\
\
            lucide.createIcons();\
            setTab('itinerary');\
            showView('detail');\
        \}\
\
        function deleteTrip(id) \{\
            trips = trips.filter(t => t.id !== id);\
            localStorage.setItem('tm_trips', JSON.stringify(trips));\
            renderTrips();\
        \}\
\
        // --- Xat ---\
        function toggleChat() \{\
            const modal = document.getElementById('chat-modal');\
            modal.classList.toggle('hidden');\
            if(!modal.classList.contains('hidden')) \{\
                const msgBox = document.getElementById('chat-messages');\
                if(msgBox.innerHTML === '') \{\
                    msgBox.innerHTML = `<div class="flex justify-start"><div class="bg-white p-4 rounded-2xl rounded-tl-none shadow-sm text-sm font-bold">Hola! S\'f3c el teu guia per a $\{currentTrip.dest\}. En qu\'e8 et puc ajudar?</div></div>`;\
                \}\
            \}\
        \}\
\
        async function sendMessage() \{\
            const input = document.getElementById('chat-input');\
            const msg = input.value.trim();\
            if(!msg) return;\
\
            const box = document.getElementById('chat-messages');\
            box.innerHTML += `<div class="flex justify-end"><div class="bg-indigo-600 text-white p-4 rounded-2xl rounded-tr-none shadow-sm text-sm font-bold">$\{msg\}</div></div>`;\
            input.value = '';\
            box.scrollTop = box.scrollHeight;\
\
            try \{\
                const response = await fetch(`https://generativelanguage.googleapis.com/v1beta/models/gemini-2.5-flash-preview-09-2025:generateContent?key=$\{apiKey\}`, \{\
                    method: 'POST',\
                    headers: \{'Content-Type': 'application/json'\},\
                    body: JSON.stringify(\{\
                        contents: [\{ parts: [\{ text: msg \}] \}],\
                        systemInstruction: \{ parts: [\{ text: `Ets un guia local expert a $\{currentTrip.dest\}. Respon de forma amable i concisa en catal\'e0. Tens aquest context: $\{JSON.stringify(currentTrip)\}` \}] \}\
                    \})\
                \});\
                const data = await response.json();\
                const aiMsg = data.candidates[0].content.parts[0].text;\
                box.innerHTML += `<div class="flex justify-start"><div class="bg-white p-4 rounded-2xl rounded-tl-none shadow-sm text-sm font-bold">$\{aiMsg\}</div></div>`;\
            \} catch(e) \{\
                box.innerHTML += `<div class="flex justify-start"><div class="bg-red-50 text-red-600 p-4 rounded-2xl shadow-sm text-sm font-bold">Error de connexi\'f3.</div></div>`;\
            \}\
            box.scrollTop = box.scrollHeight;\
        \}\
    </script>\
</body>\
</html>}